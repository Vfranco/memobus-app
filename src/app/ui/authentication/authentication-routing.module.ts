import { Routes } from '@angular/router';
import { AuthenticationViewComponent } from './view/authentication.component';

export const authenticationRouting: Routes = [
  { path: '', component: AuthenticationViewComponent }
]
